<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--<style name="Theme.StoreEcommerce" parent="android:Theme.Material.Light.NoActionBar" />-->
    <!-- Base application theme. -->
    <style name="Theme.StoreEcommerce" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--<item name="colorPrimary">@color/my_light_primary</item>-->
        <!--<item name="colorPrimary">@color/primary_color</item>-->
        <item name="android:colorAccent">@color/primary_color</item>
        <item name="android:colorPrimaryDark">@color/primary_color</item>
        <!-- System UI Configurations-->
        <item name="android:statusBarColor">@color/white</item>
        <!-- For making status bar content color dark -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <!-- For making Navigation bar content color dark -->
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

    <style name="Theme.MainActivity" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_window_background</item>
        <!-- System UI Configurations-->
        <item name="android:statusBarColor">@color/primary_color</item>
        <!-- For making status bar content color dark -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/primary_color</item>
        <!-- For making Navigation bar content color dark -->
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>

</resources>